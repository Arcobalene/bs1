# Настройка Telegram Webhook

## Проблема: команда /start не работает

Если при нажатии `/start` в Telegram боте ничего не происходит, это означает, что **webhook не настроен**. Telegram не знает, куда отправлять обновления от пользователей.

## Решение

### Шаг 1: Установите переменную окружения TELEGRAM_WEBHOOK_URL

Добавьте в ваш `.env` файл или в `docker-compose.postgres.yml`:

```bash
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/api/telegram/webhook
```

**Важно:**
- URL должен быть доступен из интернета (не localhost)
- URL должен использовать HTTPS (Telegram требует HTTPS для webhook)
- URL должен указывать на эндпоинт `/api/telegram/webhook` основного сервера

### Шаг 2: Перезапустите микросервис бота

```bash
docker-compose -f docker-compose.postgres.yml restart telegram-bot
```

### Шаг 3: Проверьте логи

После перезапуска проверьте логи микросервиса:

```bash
docker logs beauty-studio-telegram-bot --tail 50
```

Вы должны увидеть:
```
✅ Webhook успешно установлен: https://yourdomain.com/api/telegram/webhook
```

Если видите ошибку, проверьте:
- Правильность URL
- Доступность URL из интернета
- Наличие HTTPS сертификата

## Ручная установка webhook

Если автоматическая установка не работает, вы можете установить webhook вручную:

### Вариант 1: Через curl

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://yourdomain.com/api/telegram/webhook"}'
```

### Вариант 2: Через браузер

Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://yourdomain.com/api/telegram/webhook
```

Замените:
- `<YOUR_BOT_TOKEN>` на токен вашего бота
- `https://yourdomain.com` на ваш домен

## Проверка статуса webhook

### Проверить текущий webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Или в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

### Удалить webhook (если нужно)

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

## Диагностика

### Проверка 1: Webhook установлен?

Проверьте логи микросервиса при запуске:
```bash
docker logs beauty-studio-telegram-bot | grep -i webhook
```

### Проверка 2: Webhook получает запросы?

Отправьте `/start` в боте и проверьте логи:
```bash
docker logs beauty-studio-telegram-bot --tail 20
```

Должны увидеть:
```
📨 Получен webhook запрос от Telegram
📋 Update от Telegram: {...}
📝 Обработка команды /start: telegramId=...
```

### Проверка 3: Токен бота настроен?

Проверьте, что токен бота сохранен в БД или в переменных окружения:
```bash
docker logs beauty-studio-telegram-bot | grep -i "токен"
```

### Проверка 4: Микросервис доступен?

Проверьте health check:
```bash
curl http://localhost:3001/health
```

Должен вернуть:
```json
{"status":"ok"}
```

## Типичные проблемы

### 1. "Webhook не установлен"

**Причина:** Переменная `TELEGRAM_WEBHOOK_URL` не установлена или пуста.

**Решение:** Установите переменную окружения и перезапустите сервис.

### 2. "Webhook URL недоступен"

**Причина:** URL не доступен из интернета или использует HTTP вместо HTTPS.

**Решение:** 
- Убедитесь, что URL доступен из интернета
- Используйте HTTPS (Telegram требует HTTPS)
- Проверьте, что порт открыт и проксируется правильно

### 3. "Токен бота не найден"

**Причина:** Токен не сохранен в БД или не установлен в переменных окружения.

**Решение:**
- Сохраните токен в админ-панели (вкладка "Telegram")
- Или установите переменную окружения `TELEGRAM_BOT_TOKEN`

### 4. "Команда /start не обрабатывается"

**Причина:** Webhook не получает запросы или обрабатывает их неправильно.

**Решение:**
- Проверьте логи микросервиса
- Убедитесь, что webhook установлен правильно
- Проверьте, что эндпоинт `/api/telegram/webhook` доступен

## Пример настройки для production

В `docker-compose.postgres.yml`:

```yaml
telegram-bot:
  environment:
    - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    - TELEGRAM_WEBHOOK_URL=https://155.212.184.10/api/telegram/webhook
    - SALON_BASE_URL=https://155.212.184.10
```

В `.env` файле:

```bash
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_WEBHOOK_URL=https://155.212.184.10/api/telegram/webhook
SALON_BASE_URL=https://155.212.184.10
```

## После настройки

1. Отправьте `/start` в боте
2. Проверьте логи: `docker logs beauty-studio-telegram-bot --tail 20`
3. Должно появиться приветственное сообщение

Если все настроено правильно, бот должен отвечать на команду `/start`.


# 🧹 ПЛАН ОЧИСТКИ ПРОЕКТА (ЭТАП 1)

## 📋 ЧТО БУДЕТ УДАЛЕНО

### 1. Legacy монолитный код
- ❌ `server.js` (4172 строки) - старый монолит, заменён микросервисами
- ❌ `database.js` (в корне) - дубликат `shared/database.js`
- ❌ `cache.js` (в корне) - дубликат `shared/cache.js`
- ❌ `utils.js` (в корне) - дубликат `shared/utils.js`

### 2. Старые конфигурации
- ❌ `docker-compose.postgres.yml` - старая конфигурация для монолита
- ❌ `Dockerfile` (в корне) - если не используется

### 3. Утилиты (переместить/переделать)
- ⚠️ `create-admin.js` - функционал перенести в сервисы или скрипты
- ⚠️ `telegram-bot.js` (если есть) - проверить, используется ли

### 4. Документация (переместить в archive)
- 📁 Переместить все `*_FIX.md`, `*_ANALYSIS.md`, `*_DIAGNOSTICS.md` в `docs/archive/`
- 📁 Переместить старые отчеты в `docs/archive/`

---

## ✅ ПРОВЕРКА ПЕРЕД УДАЛЕНИЕМ

Перед удалением файлов проверим:
1. Нет ли импортов удаляемых файлов в коде
2. Все ли функции из старых файлов перенесены в shared/ или сервисы
3. Не используется ли старая конфигурация где-то

---

## 📁 СТРУКТУРА ПОСЛЕ ОЧИСТКИ

```
bs/
├── services/              ✅ Оставить
├── gateway/               ✅ Оставить
├── shared/                ✅ Оставить
├── views/                 ✅ Оставить
├── public/                ✅ Оставить
├── nginx/                 ✅ Оставить
├── docs/                  ✅ Создать
│   └── archive/           ✅ Создать (старая документация)
├── scripts/               ✅ Создать (для утилит)
├── docker-compose.microservices.yml  ✅ Оставить
├── package.json           ✅ Оставить
└── README.md              ✅ Оставить (обновить)
```

---

## 🚀 ПОРЯДОК ДЕЙСТВИЙ

1. ✅ Создать `docs/archive/`
2. ✅ Переместить старую документацию
3. ✅ Проверить использование файлов
4. ✅ Удалить legacy код
5. ✅ Удалить старые конфигурации
6. ✅ Создать структуру для утилит (scripts/)


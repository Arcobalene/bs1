# Чек-лист проверки функционала управления пользователями

## ✅ Проверено:

### 1. Безопасность и авторизация
- [x] Middleware `requireAuth` проверяет авторизацию
- [x] Middleware `requireAdmin` проверяет права администратора
- [x] Заблокированные пользователи не могут войти
- [x] Заблокированные пользователи автоматически разлогиниваются
- [x] Обратная совместимость: пользователи без `role` считаются обычными
- [x] Пользователь `admin` всегда считается администратором

### 2. API Endpoints
- [x] `GET /api/users` - получение списка пользователей (только для админов)
- [x] `POST /api/users/:userId/toggle` - блокировка/разблокировка
- [x] `POST /api/users/:userId/impersonate` - вход под пользователем
- [x] `POST /api/users/restore` - возврат к своему аккаунту
- [x] `DELETE /api/users/:userId` - удаление пользователя

### 3. Защита от ошибок
- [x] Нельзя заблокировать самого себя
- [x] Нельзя заблокировать другого администратора
- [x] Нельзя удалить самого себя
- [x] Нельзя удалить другого администратора
- [x] Нельзя войти под заблокированным пользователем
- [x] При удалении пользователя удаляются все его записи

### 4. Интерфейс
- [x] Страница `/users` создана и работает
- [x] Баннер impersonation отображается при входе под другим пользователем
- [x] Кнопка "Вернуться к своему аккаунту" работает
- [x] Ссылка "Пользователи" показывается только админам
- [x] Статистика пользователей отображается (услуги, мастера)

### 5. Регистрация и вход
- [x] Новые пользователи получают `role: 'user'` и `isActive: true`
- [x] Демо-аккаунт создается с `role: 'admin'` и `isActive: true`
- [x] При входе проверяется блокировка аккаунта
- [x] При входе сохраняется `originalUserId` для impersonation

### 6. Обратная совместимость
- [x] Старые пользователи без `role` считаются обычными пользователями
- [x] Старые пользователи без `isActive` считаются активными
- [x] Пользователь `admin` всегда имеет права администратора

## 🔍 Что проверить вручную:

1. **Вход как администратор:**
   - Войти с `admin` / `admin123`
   - Проверить, что видна ссылка "Пользователи"

2. **Управление пользователями:**
   - Открыть страницу `/users`
   - Проверить список всех пользователей
   - Попробовать заблокировать пользователя
   - Попробовать войти под пользователем
   - Проверить баннер impersonation
   - Вернуться к своему аккаунту

3. **Безопасность:**
   - Попробовать заблокировать самого себя (должна быть ошибка)
   - Попробовать удалить самого себя (должна быть ошибка)
   - Попробовать заблокировать другого админа (должна быть ошибка)

4. **Блокировка:**
   - Заблокировать пользователя
   - Попробовать войти под заблокированным пользователем (должна быть ошибка)
   - Разблокировать пользователя
   - Проверить, что теперь можно войти


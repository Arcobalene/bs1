# Beauty Studio — Система онлайн-записи

Полноценная система онлайн-записи для салона красоты с индивидуальными аккаунтами для каждого владельца салона.

## Возможности

- ✅ **Индивидуальные аккаунты** — каждый владелец салона имеет свой аккаунт
- ✅ **Управление услугами** — настройка цен и длительности услуг
- ✅ **Управление мастерами** — добавление и редактирование мастеров
- ✅ **Онлайн-запись** — публичная форма записи для клиентов
- ✅ **Календарь записей** — недельный календарь с просмотром всех записей
- ✅ **Защита данных** — пароли хранятся в зашифрованном виде
- ✅ **Персональные ссылки** — у каждого салона своя ссылка для записи

## Технологии

- **Backend:** Node.js + Express
- **База данных:** SQLite (better-sqlite3) - встроена в Docker контейнер
- **Аутентификация:** express-session + bcrypt
- **Frontend:** HTML, CSS, JavaScript
- **Карты:** Яндекс.Карты API
- **Контейнеризация:** Docker + Docker Compose

## Установка

### 🐳 Вариант 1: Docker (рекомендуется)

**Для Windows 11:**
1. Установите [Docker Desktop](https://www.docker.com/products/docker-desktop)
2. Дважды кликните на **`docker-start.bat`**
3. Откройте браузер: `http://localhost:3000`

**Через командную строку:**
```bash
docker-compose up -d --build
```

**Остановка:**
- Windows: дважды кликните на `docker-stop.bat`
- Или: `docker-compose down`

### 💻 Вариант 2: Локальная установка

**Для Windows 11 (простой способ):**

1. **Дважды кликните на `install.bat`** — это установит все зависимости
2. **Дважды кликните на `start.bat`** — это запустит сервер
3. Откройте в браузере: `http://localhost:3000`

**Для Windows (через командную строку):**

1. Откройте PowerShell или CMD в папке проекта
2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm start
```

4. Откройте в браузере:
```
http://localhost:3000
```

### Требования:

**Для Docker:**
- Docker Desktop для Windows
- Скачать: https://www.docker.com/products/docker-desktop

**Для локальной установки:**
- **Node.js** версии 14 или выше
  - Скачать: https://nodejs.org/
  - Выберите LTS версию
  - После установки перезапустите терминал

## Использование

### Для владельцев салонов

#### Быстрый старт (демо-аккаунт):

При первом запуске автоматически создается демо-аккаунт:
- **Логин:** `admin`
- **Пароль:** `admin123`

1. Откройте `http://localhost:3000/login`
2. Введите логин и пароль выше
3. Вы попадете в админ-панель

#### Или создайте свой аккаунт:

1. **Регистрация**: Перейдите на `/register` и создайте аккаунт
2. **Вход**: Войдите в систему через `/login`
3. **Админ-панель**: Настройте услуги и мастеров в `/admin`
4. **Календарь**: Просматривайте записи в `/calendar`
5. **Ссылка для клиентов**: Скопируйте вашу персональную ссылку из админ-панели

### Для клиентов

1. Перейдите по ссылке салона (например: `http://localhost:3000/?userId=1`)
2. Выберите услугу и мастера
3. Выберите дату и время
4. Заполните форму и отправьте заявку

## Структура проекта

```
bs/
├── server.js          # Основной сервер
├── package.json       # Зависимости
├── data/              # База данных (JSON файлы)
│   ├── users.json     # Пользователи
│   └── bookings.json  # Записи
├── views/             # HTML страницы
│   ├── index.html     # Главная (форма записи)
│   ├── login.html     # Вход
│   ├── register.html  # Регистрация
│   ├── admin.html     # Админ-панель
│   └── calendar.html  # Календарь
└── public/            # Статические файлы
    ├── style.css      # Стили
    └── app.js         # Общие функции
```

## API Endpoints

### Публичные
- `GET /` — Главная страница
- `GET /api/services/:userId` — Получить услуги пользователя
- `GET /api/masters/:userId` — Получить мастеров пользователя
- `POST /api/bookings` — Создать запись

### Требуют авторизации
- `GET /api/user` — Получить данные пользователя
- `POST /api/services` — Обновить услуги
- `POST /api/masters` — Обновить мастеров
- `GET /api/bookings` — Получить записи пользователя
- `POST /api/login` — Вход
- `POST /api/register` — Регистрация
- `POST /api/logout` — Выход

## Безопасность

- Пароли хранятся в зашифрованном виде (bcrypt)
- Сессии защищены
- Админ-панель доступна только авторизованным пользователям
- Каждый пользователь видит только свои данные

## Технологии

- **Backend**: Node.js + Express
- **База данных**: JSON файлы (можно легко заменить на MongoDB/PostgreSQL)
- **Аутентификация**: express-session + bcrypt
- **Frontend**: Vanilla JavaScript

## Примечания

- Данные хранятся в JSON файлах в папке `data/`
- Для продакшн-версии рекомендуется использовать настоящую БД
- Измените секретный ключ сессии в `server.js` для продакшн
- При использовании Docker данные сохраняются в папке `data/` на хосте

## Дополнительная документация

- **CREDENTIALS.md** — учетные данные для входа (демо-аккаунт)
- **WINDOWS.md** — подробная инструкция для Windows 11
- **DOCKER.md** — инструкция по работе с Docker
- **START.md** — быстрый старт


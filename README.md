# Beauty Studio - Система онлайн-записи для салона красоты

Микросервисная архитектура для управления онлайн-записями в салонах красоты.

## 🏗️ Архитектура

Проект построен на микросервисной архитектуре:

- **Gateway** (`gateway/`) - API Gateway, единая точка входа для всех запросов
- **Auth Service** (`services/auth-service/`) - Аутентификация и авторизация
- **User Service** (`services/user-service/`) - Управление пользователями и салонами
- **Booking Service** (`services/booking-service/`) - Управление записями
- **Catalog Service** (`services/catalog-service/`) - Управление услугами и мастерами
- **File Service** (`services/file-service/`) - Хранение файлов (MinIO)
- **Notification Service** (`services/notification-service/`) - Уведомления
- **Telegram Service** (`services/telegram-service/`) - Интеграция с Telegram

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Node.js 18+ (для локальной разработки)

### Запуск через Docker Compose

1. Скопируйте пример конфигурации:
```bash
cp env.example .env
```

2. Отредактируйте `.env` файл, установив необходимые значения (особенно `SESSION_SECRET`)

3. Запустите все сервисы:
```bash
docker-compose -f docker-compose.microservices.yml up -d
```

4. Проверьте статус:
```bash
docker-compose -f docker-compose.microservices.yml ps
```

5. Просмотрите логи:
```bash
docker-compose -f docker-compose.microservices.yml logs -f gateway
```

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные окружения (см. `env.example`)

3. Запустите сервисы локально (требуется запущенная БД и Redis)

## 📁 Структура проекта

```
bs/
├── gateway/              # API Gateway
│   ├── Dockerfile
│   ├── package.json
│   └── server.js
├── services/             # Микросервисы
│   ├── auth-service/
│   ├── user-service/
│   ├── booking-service/
│   ├── catalog-service/
│   ├── file-service/
│   ├── notification-service/
│   └── telegram-service/
├── shared/               # Общий код
│   ├── database.js       # Подключение к БД
│   ├── utils.js          # Утилиты
│   ├── cache.js          # Кэширование
│   ├── config.js         # Валидация конфигурации
│   ├── logger.js         # Логирование
│   └── errors.js         # Обработка ошибок
├── nginx/                # Конфигурация Nginx
│   └── nginx.conf
├── views/                # HTML страницы
├── public/               # Статические файлы
├── docker-compose.microservices.yml
└── package.json
```

## 🔒 Безопасность

- Все пароли хэшируются с помощью bcrypt
- Используется rate limiting для защиты от брутфорса
- Helmet для защиты HTTP заголовков
- Валидация всех входящих данных
- Параметризованные SQL запросы для защиты от SQL-инъекций
- Сессии хранятся в Redis

## 🛠️ Разработка

### Линтинг и форматирование

```bash
npm run lint          # Проверка кода
npm run lint:fix      # Автоисправление
npm run format        # Форматирование с Prettier
```

### Аудит безопасности

```bash
npm run audit         # Проверка уязвимостей
```

## 📝 Переменные окружения

См. `env.example` для полного списка переменных окружения.

Обязательные переменные:
- `SESSION_SECRET` - секретный ключ для сессий (ОБЯЗАТЕЛЬНО измените в production!)
- `DB_PASSWORD` - пароль базы данных
- `DB_NAME`, `DB_USER`, `DB_HOST`, `DB_PORT` - параметры подключения к БД

## 🐳 Docker

### Оптимизация образов

Все Dockerfile используют multi-stage builds для уменьшения размера образов.

### Health Checks

Все сервисы имеют health checks для мониторинга состояния.

## 📚 API Документация

API документация будет доступна по адресу `/api-docs` (планируется интеграция Swagger).

## 🤝 Вклад в проект

1. Создайте ветку для новой функции
2. Внесите изменения
3. Убедитесь, что код проходит линтинг
4. Создайте Pull Request

## 📄 Лицензия

ISC

